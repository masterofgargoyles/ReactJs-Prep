function whosPaying(names) {
    
    /******Don't change the code above*******/
    var length = names.length;
    var name = Math.floor(Math.random()*length);
    return (names[name]+" is going to buy lunch today!");
        
    /******Don't change the code below*******/    
    }